<UserControl x:Class="TestDepedencyProperties.CustomUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" DataContext="{Binding RelativeSource={x:Static RelativeSource.Self}}">
    <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Label Content="Text DependencyProperty (should be same as above and below):" />
        <Border BorderThickness="1" BorderBrush="Black" MinWidth="150">
            <TextBlock Text="{Binding Text}" />
        </Border>
    </WrapPanel>
</UserControl>
